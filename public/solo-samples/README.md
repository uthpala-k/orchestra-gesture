# Solo samples

Downloaded solo sample files are intentionally excluded from Git.

Install them locally with:

```powershell
npm run install:solo-samples
```

The installer also creates the attribution notice required for the downloaded
sample set.
